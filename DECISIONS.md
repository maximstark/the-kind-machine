# DECISIONS — overnight build log

One line each: decision + why + what to review.

## P7 pass
- **Ending source = the clean (accept) generation; the keep-regen with the baked cross is unused** — one source image, two engine treatments is the architecture (tint + damage in code), and a baked cross would double with the engine's. The engine's cross is now dry-brushed (3 fixed-offset passes — fixed because the epitaph redraws every frame) to sit in the art's stroke language. Review: keep-cross look on the epitaph.
- **Avatar threshold 0.55** (probe sweep on your generation): the hairline seam appears and features carve deepest while the face stays solid — the contrast a ~23px in-world read needs. Review: bust read on your phone in the field.
- **Hero PNGs ship-encoded via `tools/shrink-asset.mjs`** (1280px cap — the engine's own processing cap — + posterize to 16 gray levels + re-encode): 3.25MB → 353KB. The 1-bit threshold only cares about values near the cutoff, so posterize is visually free; raw generations stay in notes/. Review: none, parity verified.
- **OG card now composes the real title art** (isolated no-type render), per the P6 review note that the hall shot was a stand-in. Review: the crop (arch breaks frame, pool + figure in frame) before the launch post.
- **Card isolation renders = `node tools/render-cards.mjs` → `notes/card-*.png`** (dev-only `/cards.html`; quantizer factored into `src/tools/quantize.ts`, shared with the sprite sheet). Renders the hero slots through the real heroAssets thresholds + the shipped epitaph treatments, minus type — editing bases that round-trip: overwrite `src/assets/`, re-run, compare. Review: whether the title's 0.9-alpha gold speckle should be in the base or only in-game.
- **Tower glyphs stay sculptural, scaled 1.5× (mounted higher to clear the plinth)** — Maxim's call July 4: legibility from size, not from a generated glyph set; chapel's foreshadowing row inherits the scale at its 0.8 modifier so the hierarchy holds. Review: read the four kinds at tower spawn distance on your phone; brief #4 (drawn glyph set) stays open as a stretch if scale isn't enough.
- **Wanderer sprite sheet for Maxim's imagegen passes = `node tools/render-character.mjs` → `notes/character-sheet.png`** (dev-only `/sheet.html` + `src/tools/sheet.ts`; never in the build). Drives the real Character class through its public API so poses are the shipped animation, not approximations; rows 1–3 add fill light (the game rig renders the figure near-silhouette, useless as an editing base), row 4 keeps ground truth — orthos, close-up, and an in-game-scale Bayer+quantize pair whose JS quantizer replicates the composite shader including its display-vs-working-space palette quirk. Review: whether the sheet gives your AI tools enough form to keep the silhouette on-model.
- **Hero ingestion crushes to 1-bit on the CPU at load, not in-shader** — the composite pass stays one shader with no new uniforms; the mask tints per use on the UI canvas (title bone, epitaph gold/bone) and doubles as a world texture, and no generation can smuggle a sixth color past the palette. Per-asset thresholds live in the `heroAssets.ts` manifest; `node tools/probe-threshold.mjs <file>` renders an 11-step sweep for tuning by eye. Review: thresholds once real generations land.
- **Committed placeholder PNGs in `src/assets/` instead of empty slots** (via `tools/make-placeholders.mjs`, composed to the briefs) — the ingestion path ships exercised by every playthrough rather than dormant until launch week; drop-in = overwrite the same filename. Review: placeholders are knowingly crude; they exist to be replaced.
- **Title card = cover-fit behind the type with darkened text bands**; drawn at 0.9 alpha, bone deliberately picks up gold Bayer speckle over black — tarnished, not clean. Review: band opacity against your real card's composition.
- **ARCHIVIST avatar = billboarded 1-bit quad replacing the bust's primitive head** (plinth + shoulders stay), sitting inside the locus smear that already lives at every mark; net −3 draw calls per scene. Review: quad size (0.78 wu) — presence vs. the never-resolve rule.
- **Ending illustration replaces the chalk sigil on the epitaph; the crossing-out survives on top of the art** (accept = gold and clean, keep = bone, crossed, ash-scratched) — the palette shift carries both endings from one image per the brief. Review: whether losing the bare chalk circle from the card matters to you.
- **Entry variants = deferent/defiant only; wavering keeps the base line; the field gets none** — no answers exist yet when the field line plays, and the wavering middle is what the base lines were already written for. Review: the six DRAFT variants (chapel/tower/hall).
- **Proxy hardened to config-only deploy**: origin allowlist defaulting to the live Pages domain, per-IP sliding window (40 per 10 min — several full runs, nothing hotter), 8KB body cap, server-side one-line gate mirroring the client's, `/healthz`. In-memory limiter is deliberate for a single-instance Coolify box. Review: the rate numbers.

## P6 pass
- **OG/social card = the hall shot (beam + gold door) with title left, composed at 1200×630** via `tools/og-card.html` + `tools/render-og.mjs` → committed as `public/og.png`; absolute URLs in index.html meta since OG scrapers require them. Review: swap the base image for the §7c title card when it exists — it's the intended launch image.
- **Four machine-voice audio treatments behind `?voice=`** (reed/liturgy/presence/choir; default unchanged grain) — robotic-calm comes from *removing* randomness (reed), pitch order (liturgy chants a fixed Am9 cycle, punctuation lands the root), or a breathing bed that fades in only while it speaks (presence). Deterministic on purpose: sameness is what reads as machine. Review: A/B on your phone, pick one, I delete the rest.
- **`machine:tick` now carries the character; `machine:reveal-done` emitted when a line finishes revealing** (timer or skip) — lets audio give punctuation weight and lets the speech bed track actual speaking, not the hold. Review: none, behavior identical with default voice.
- **Hero assets pivot to Maxim's AI generations** (scope §7c said title card "should be your hand") — his call July 3; thematically self-consistent (machine-reconstructed art for a machine that reconstructs). Generation briefs in `notes/asset-briefs.md`; ingestion pipeline (threshold-to-1-bit, smear slot, palette-shift) is the next build. Review: keep one human mark in the launch post.
- **Two voice-like modes added after phone feedback ("I don't hear a voice"): `vox` (wordless formant babble — the text's own vowels pick the mouth shape, monotone 110Hz, falling inflection at punctuation) and `hum` (sustained vowel hum that morphs while text reveals, reed tick typing over it).** Both are pure synthesis — the scope's no-VO rule holds; a voice-*like* presence without words is the middle path. Liturgy/bed levels also raised (+~50%), they were under the drone. Review: vox syllable rate (every 2nd char) and whether monotone reads calm or dead.
- **`voice` mode = vox babble over the hum bed** (Maxim's pick after hearing both), vox levels +60% (they sat under the drone on phone speakers), hum ducked to 0.22 under the babble so the "speech" stays on top. Review: this is the leading candidate — phone-check the mix in the chapel (busiest sound moment: drone darker + quiz chimes), then I delete the losing modes.
- **`voice` promoted to THE machine voice; all seven losing modes and the `?voice=` A/B plumbing deleted** (Maxim confirmed July 3: "that's perfect"). The old typewriter grain is gone entirely — the babble is the tick now. Also approved: one signature processed spoken line for the finale (scope amendment to §6 "no VO" — a single asset, everything else stays synthesized). Review: none; A/B is over.
- **ARCHIVIST spoken-line chain = `tools/process-voice.mjs`** — Web Audio inside headless Chrome (same primitives as the live synth): normalize/trim -> granular smear (90ms grains, deterministic detune walk) -> subtle 110Hz ring-mod -> EQ toward the babble's dark vowels -> 10-bit quantize mix -> 3.5s dark convolver -> compressor. Parameter-deterministic (seeded); renders are audibly identical but not bit-exact (Chrome convolver threading). Raw takes + renders live in gitignored notes/; only a final encoded asset will ship. Review: listen to notes/archivist-line-v1.wav; knobs are constants at each stage.
- **`--preset dark` variant for the accept ending** (Maxim: "excruciatingly dark for the ending where we trust the machine") — the screen goes bright and absolved while the voice underneath goes abyssal: 1.18x stretch, −350c pitch + octave-deep shadow layer, 55Hz carrier, 9-bit crush, 6s darker hall, grain jitter/detune widening as the line completes (it disintegrates as it finishes), and a reverse-reverb pre-echo — the room answers each phrase *before* it is spoken, because the archive already contains it. v1 (`default`) untouched and regression-checked. Review: notes/archivist-line-v2-dark.wav — intelligibility vs. dread balance; every knob is in the PRESETS block.
- **The spoken finale line is wired in** ("thank you for helping me remember" confirmed as final wording — the take already said it). Keep → `default` render, accept → `dark` render, from `public/voice/` (16kHz mono WAV via `tools/resample.mjs`; content is lowpassed ≤3.4kHz so 16k is transparent; no ffmpeg on this machine, AAC later if size ever matters). Prefetch fires at hall entry so the finale never waits on network; the babble mutes only while the take plays; the dark render's 6s tail deliberately bleeds through the ink-out into the title. On a return visit the text says "Again." but the audio is the identical take — the thanks is literally a recording, replayed; that is the point. Review: the audio/text mismatch on run 2 — I think it's the feature, veto if it reads as a bug.
- **The machine remembers you between runs** (Maxim: "too good to skip") — `archive.ts` stores the last completed run's summary in localStorage, recorded at the moment of choice. Exactly three touches, all gentle, none smug: cold open +1 line ("Remembering is my part."), one ending-specific field beat, and the finale becomes "Thank you for helping me remember. **Again.**" The original clean-reload stays for run one; storage failure = graceful amnesia. This makes the "you were being archived" hook literal and rewards the player who replays to check whether the game changes — the game noticed them checking. Review: the four returnVisit DRAFT lines in script.ts; whether three touches is the right dose (I chose restraint — the horror is that it's matter-of-fact).

## P2 pass
- **Character is code-animated primitives, not imported assets.** At 360px, silhouette + motion (springs, lag, weight) beat mesh detail; no Blender dependency; a modeled mesh can drop into the same hierarchy later. Review: walk feel on your phone — tuning constants live at the top of character.ts.
- **Cold open states the situation, withholds only cause and blame.** The scope doc forbids stating the theme, not the premise; the logline itself is player-facing. Review: the five coldOpen lines in script.ts.
- **Waymark gate = 2 examines or 75s**, announced when it opens; taps from afar walk you to it. Gives the observe phase a shape without a tutorial popup. Review: gate thresholds.
- **ARCHIVIST locus**: bust at every mark + a screen-space smear radius around it in the composite pass — reality least stable where the machine is most present. The hall keeps the beam as the locus instead. Review: bust silhouette (placeholder for your §7c avatar).
- **Green quantization now requires the UI pixel to be green-dominant** — translucent bone UI over warm surfaces was leaking machine-green into the world.
- **Attention system doubles as guidance**: the character glances at the nearest examinable (and the mark once it's lit). Subtle, diegetic, no UI.

- **Local git only, no remote.** Commit-early rule needs a repo; no remote was specified. Review: push target.
- **Flourish stub = Vite dev middleware at `/api/flourish`.** Keeps `npm run dev` one command while matching the real proxy's HTTP contract exactly; `proxy/` holds the unwired Hono skeleton. Review: contract shape before real integration.
- **All UI drawn in-pipeline** (2D canvas at RT resolution, composited in the post shader pre-quantization). Text, cards, captions all inherit the dither/palette and can be smeared by the corruption layer; machine green is shader-gated to UI pixels only, so it can never appear in the world. Review: text legibility on a real phone.
- **Linear fog calibrated to camera distance** (near/far as offsets from the iso rig distance) instead of exp2 — exp2 at iso distances crushed everything to black. Review: none, it's a graybox-visible win.
- **Dither spread 0.34** after trying 0.2 (flat slabs) and 0.5 (uniform noise). This is the "gradients read as crosshatch, fills stay quiet" middle. Review: judge on phone screen.
- **Palette ash retuned** from 0x77705f (warm olive — dithered to brown mud against black) to 0x7e7b72 (neutral). Review: against your reference set.
- **Machine text = Georgia serif, player captions = Consolas mono**, both with dark backing plates. Serif-through-dither reads "cathedral"; mono reads "record". Review: font choice is yours to veto.
- **Character = primitive robed figure** (cone + bone head sphere), not billboard sprite — reads well at 360px, zero art dependency. Review: §6 allowed either; swap to sprite later if you prefer.
- **Screenshot verification via Playwright (channel: chrome, headless)** — `npm run shot`. Nothing opens on your desktop.
- **Scene 1 has no lie and no mutation.** §2 says every scene has one of each; §3's arc says the Field machine is "warm, accurate, trustworthy." I followed the arc — trust must exist before it can be spent. Review: if you want a Field mutation, flip one flag in `field.ts`.
- **Re-entry rewrite adopts the machine's version into ground truth** (ledger `redraw`) when the player agreed with the lie — so re-examining matches the redrawn world. The quiz record still holds the original truth for the ending assembly. Review: this makes deference literally rewrite reality; I believe that's the intent of §5.5.
- **Mutation trigger**: fires on any examine while the target detail is off-screen/far (sky details shift on any examine); guaranteed fallback at dissolve-out if never triggered. Review: tune per scene on phone playtest.
- **Tower puzzle forgiveness**: following the machine's false order twice opens the door anyway (with the machine admitting fault — the honest-after-caught beat). Wrong non-matching orders just reset silently. Review: fail counts.
- **Personal (unverifiable) quiz details** in the Tower have `mutable: false` and no world anchor — the "record" is the only witness; only the beacon can truly mutate. Review: the doc implies this but doesn't say it.
- **Ending endgame flow**: choice → per-ending scene lighting + atmosphere (accept = becalmed + brightened, keep = heavier baseline) → single thank-you line → 4.5s ink-out → title; tapping title after a finished run reloads the page for a clean second playthrough.
- **Iso screen axes**: north (−z) projects up-RIGHT at az 45°; scene content is laid out along the screen diagonal (tower moved west) rather than world-north. Cameras get per-scene `camBias` to lead toward the scene's heart.
- **The hall runs along the screen-up diagonal** so the whole walk reads vertically in portrait — lone figure, colonnade, beam, door, all in one axis. The pullback is dynamic: view width 14 → 31 as a function of progress down the hall (`viewWidthAt`).
- **Waymark taps are distance-gated**: tapping the mark from afar walks you to it, then triggers. The mark is met, not commanded.
- **Flourish gate keeps only the first sentence** of whatever the endpoint returns (length ≤160, no brackets/newlines) — the stub's multi-sentence canned lines get trimmed; that's the gate working, not a bug.
- **Bundle is one chunk (~195KB gz)** — three.js dominates; fine for a load-once mobile vignette. Code-split only if Threads' browser balks.
