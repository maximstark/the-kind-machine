{
  "name": "tkm-flourish-proxy",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Flourish proxy for THE KIND MACHINE. Separate deploy; not part of the client build. Requires ANTHROPIC_API_KEY and GAME_ORIGIN env vars.",
  "scripts": {
    "start": "node --experimental-strip-types server.ts"
  },
  "dependencies": {
    "hono": "^4.5.0",
    "@hono/node-server": "^1.12.0"
  }
}
