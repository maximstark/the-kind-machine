# STATUS — overnight build

Updated as systems land. Read DECISIONS.md alongside this.

## Done
- **M1: Pixel/dither pipeline + iso camera + scaffold.** 360px RT, nearest upscale, 4×4 Bayer, 5-color palette, perceptual (redmean) quantization, shader vignette, UI-gated machine green, in-pipeline canvas UI. Judge gate passed — see `shots/m1-final.png`.

## In progress
- M2: grid + tap-to-move + examine + ledger.

## Stubbed
- `/api/flourish` = Vite dev middleware with canned DRAFT lines + realistic latency.

## Broken
- (nothing known)

## Next
- M2 → M3 (dissolve/quiz/trust) → M4 (corruption) → M5 (scenes/endings) → M6 (audio/flourish) → M7 (Scene 4 monumental).
